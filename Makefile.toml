[config]
default_to_workspace = false

[tasks.run-vm]
script = ['''
  set -euo pipefail
  TARGET_BASE="$(cargo metadata --format-version 1 --no-deps | jq -r '.target_directory')"
  MUSL_TARGET="x86_64-unknown-linux-musl"
  cargo build --release --target "${MUSL_TARGET}" -p patchbay-runner --bin patchbay --example transfer
  cargo run -p patchbay-vm -- run --patchbay-version "path:${TARGET_BASE}/${MUSL_TARGET}/release/patchbay" ${@}
  ''']

[tasks.test-vm]
script = ['''
  set -euo pipefail
  cargo run -p patchbay-vm -- test --target "x86_64-unknown-linux-musl" ${@}
  ''']

# Format every workspace member, excluding pinned third-party path dependencies.
[tasks.format]
workspace = false
command = "cargo"
args = [
     "fmt",
     "--package", "patchbay",
     "--package", "patchbay-utils",
     "--package", "patchbay-runner",
     "--package", "patchbay-vm",
     "--package", "patchbay-server",
     "--package", "patchbay-cli",
     "--",
     "--config",
     "unstable_features=true",
     "--config",
     "imports_granularity=Crate,group_imports=StdExternalCrate,reorder_imports=true,format_code_in_doc_comments=true",
]

[tasks.format-check]
workspace = false
command = "cargo"
args = [
     "fmt",
     "--package", "patchbay",
     "--package", "patchbay-utils",
     "--package", "patchbay-runner",
     "--package", "patchbay-vm",
     "--package", "patchbay-server",
     "--package", "patchbay-cli",
     "--check",
     "--",
     "--config",
     "unstable_features=true",
     "--config",
     "imports_granularity=Crate,group_imports=StdExternalCrate,reorder_imports=true,format_code_in_doc_comments=true",
]
